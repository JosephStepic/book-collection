<%= form_with(model: book3) do |form| %>
  <% if book3.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(book3.errors.count, "error") %> prohibited this book3 from being saved:</h2>

      <ul>
        <% book3.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :author %>
    <%= form.text_field :author %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.text_field :price %>
  </div>

  <div class="field">
    <%= form.label :published_date %>
    <%= form.date_select :published_date %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
